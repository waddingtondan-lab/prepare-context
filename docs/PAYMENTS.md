# Payments (future — not in MVP)

Prepare Context MVP is free/local and requires **no API key**.

Planned (do not implement yet):

1. **x402 / USDC per-request** — pay-per-call at `https://prepare.plaintools.vip` for anonymous or agent wallets
2. **API-key credits** — prepaid token budgets for builders who prefer keys over on-chain micropayments

This stub exists so agents and builders know monetization will land later without guessing. The scrub/compress path ships first.
